## We have the following tab on this application "Country insights"
We would like to change the layout 

What is available:
1. enhanced_import_data, enhanced_export_data

What we need is the following in the "country insights" tab
1. Sort the total trade and yoy growth
2. add two sections next to trading partners "import", "exports"
3. the initial visualization for imports should be the 
    - aggregate imports over time from the data source (total observed value by time)
    - above each bar should be the %change from the previous month
    - another visualization of a horizontal bar chart should be there
    - we need to filter by country.  for each country the charts should adjust

feel free to only use on of the files (either import or export)

